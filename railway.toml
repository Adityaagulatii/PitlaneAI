[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && cd \"PitLane AI Web Application\" && npm install && npm run build"

[deploy]
startCommand = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
